export interface approvedRequestForEvent {
  requestId: number;
  tanNumber: string;
  noOfParticipants: number;
  requestDate: string;
  justification: string;
  requestedBy: string;
  department: string;
}
